# PocketBase Configuration
VITE_POCKETBASE_URL=http://127.0.0.1:8098
VITE_POCKETBASE_URL=http://3.101.63.121:8098
VITE_API_URL=http://127.0.0.1:8098/api

# App Configuration
VITE_APP_NAME=The Language Network
VITE_APP_URL=http://localhost:5173

# Admin Credentials (for reference only - do not commit to git!)
pocketbase_admin=mainaksaha0807@gmail.com
pocketbase_password=8104760831

# SSH into AWS
ssh -i /Users/mainaksaha/Desktop/MASTERS/Project/TLN/language-network/TLN.pem ec2-user@3.101.63.121

# Clean rebuild to ensure new environment variables are used
rm -rf dist/
npm run build

# Verify the build contains correct URLs
grep -r "thelanguagenetwork.co" dist/

# Deploy the updated build
sudo cp -r dist/* /var/www/html/
sudo chown -R nginx:nginx /var/www/html